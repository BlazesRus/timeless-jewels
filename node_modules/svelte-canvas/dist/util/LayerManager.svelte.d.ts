import type { CanvasContext, CanvasConfig, LayerProps } from '../types';
declare class LayerManager {
    #private;
    constructor(config: CanvasConfig);
    init(context: CanvasContext, layerRef: HTMLElement): void;
    redraw(): void;
    register({ setup, render, ...eventHandlers }: LayerProps): number;
    createEventHandlers(): {};
}
export default LayerManager;
